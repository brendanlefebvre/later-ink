services:
  later-ink:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/data/app.db
    volumes:
      - ./data:/data
    restart: unless-stopped
